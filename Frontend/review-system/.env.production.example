# Copy values into Vercel (or your host) as environment variables — do not commit secrets here.
# Production API (Render or other). No trailing slash.
REACT_APP_API_URL=https://your-chainproof-api.onrender.com
